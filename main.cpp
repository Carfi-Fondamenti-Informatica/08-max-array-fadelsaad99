#include <iostream>
#include "lib.h"
using namespace std;

int main() {
    int dim;
    cin>>dim;
    float a[dim];
    float x;
    for(int i=0; i<h; i++){
        cin>>x;
        a[dim]=x;
    }
    cout<<mass(a,dim);
    return 0;
}
